/**
 * §B.3 · the content inset. `32px 40px` on every route; `28px 40px` on a route
 * whose `main` is a single full-height working panel, with the 8px going to the
 * panel; `24px 28px` at counter density. This is not a per-screen judgement --
 * a route with one panel takes 28, a route with a grid takes 32.
 */
export const CONTENT_INSET = "px-10 py-8";
export const PANEL_INSET = "px-10 py-7";
export const COUNTER_INSET = "px-7 py-6";
